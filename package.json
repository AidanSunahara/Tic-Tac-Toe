{
  "name": "simple-proj",
  "version": "1.0.0",
  "description": "intro",
  "main": "index.html",
  "scripts": {
    "start": "npm-run-all --parallel lite tailwind",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite": "lite-server",
    "tailwind": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
  },
  "author": "Aidan Sunahara",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.2.4"
  },
  "dependencies": {
    "node-modules": "^1.0.1"
  }
}
